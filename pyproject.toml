[project]
name = "ruckus-ap-homeracker-mount-openscad"
version = "0.1.0"
description = "OpenSCAD model for mounting Ruckus APs to a HomeRacker bar."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "scadm==0.7.0",
]
