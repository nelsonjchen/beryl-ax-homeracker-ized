[project]
name = "beryl-ax-homeracker-sleeve-openscad"
version = "0.1.0"
description = "OpenSCAD model for adapting a GL.iNet Beryl AX holster to a HomeRacker bar."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "scadm==0.7.0",
]
